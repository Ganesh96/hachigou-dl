# Hachigou Backend

FastAPI backend for Hachigou DL.

## Component 1

Implemented:

- `GET /health`
- Environment-based settings
- CORS for frontend access
- Docker-ready backend

## Local run

Install dependencies:

```bash
python -m pip install -r requirements.txt